export enum BookingStatusEnum {
  PROCESSING = 1,
  BOOKED = 2,
  CANCELED = 3,
  PREPARING = 4,
  WAITINGCAR = 5,
  USING = 6,
  RETURNTIME = 7,
  RETURNED = 8,
  OVERTIME = 9,
}

export enum CommissionRate {
  RATE = 0.05,
}
